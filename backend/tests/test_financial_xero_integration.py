"""
RED phase fixtures for Xero + ratio engine integration (DEV-010).
Currently skipped until implementation is ready.
"""

import pytest


@pytest.mark.skip(reason="To be implemented in ratio engine phase")
def test_placeholder_ratio_engine():
    assert False

