[coverage]
source = backend/app
omit = backend/venv/*
[pytest]
testpaths =
    backend/tests
addopts = -ra
norecursedirs = venv .git .tox dist build *.egg nul
# Fix for Windows: Explicitly set testpaths to prevent pytest from scanning
# parent directories and hitting the 'nul' Windows device file
