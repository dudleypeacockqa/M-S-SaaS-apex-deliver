/**
 * Centralized icon exports.
 *
 * Components should import from  so we can swap icon providers in
 * one place without touching every file.
 */

export * from 'lucide-react'
export type { LucideIcon } from 'lucide-react'
